export type Shop = {
  name: string;
  tag: string;
};

export type Category = {
  emoji: string;
  label: string;
};

export type FamilyMember = {
  initial: string;
  name: string;
  color: string;
};